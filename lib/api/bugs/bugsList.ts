const userCreateErrorLog = 'Invalid `userId` param: Parameter must contain at most 36 chars. Valid chars are a-z, A-Z, 0-9, period, hyphen, and underscore. Can\'t start with a special char'

export const bugsList = {
    userCreateErrorLog
}